{
  "name": "handcontrol-ai",
  "version": "3.0.0",
  "private": true,
  "description": "Sistema de control por gestos con IA — HandControl AI",
  "scripts": {
    "frontend":    "cd frontend && npm run dev",
    "backend":     "cd backend && node index.js",
    "dev":         "start \"Frontend\" cmd /k \"cd frontend && npm run dev\" && cd backend && node index.js",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "build":       "cd frontend && npm run build"
  }
}
